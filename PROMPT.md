# obsidian-dart-plugin 프로그램 명세서 v0.1

## 1. 목적

`obsidian-dart-plugin`은 Obsidian에서 DART 공시 URL을 입력하면 해당 공시에서 선택한 정보를 추출하고, 현재 노트의 커서 위치에 마크다운 테이블을 삽입하는 플러그인이다.

초기 버전은 다음 항목을 지원한다.

| 항목        | 설명                              |
| --------- | ------------------------------- |
| 연결포괄손익계산서 | 연결 기준 포괄손익계산서에서 매출, 영업이익을 추출한다. |
| 포괄손익계산서   | 별도 기준 포괄손익계산서에서 매출, 영업이익을 추출한다. |
| 주식의 총수    | 공시에서 주식 수 관련 정보를 추출한다.          |

## 2. 플러그인 이름

플러그인 이름은 다음과 같다.

```text
obsidian-dart-plugin
```

패키지명, 저장소명, 플러그인 ID도 동일하게 사용한다.

## 3. 기본 사용 흐름

사용자는 Obsidian 왼쪽 Ribbon 영역의 플러그인 버튼을 클릭한다.

플러그인은 입력 창을 표시한다.

사용자는 DART API Key, DART 공시 URL, 추출 항목, 금액 단위를 입력하거나 선택한다.

사용자가 실행 버튼을 누르면 플러그인은 공시 데이터를 조회하고, 선택한 항목을 마크다운 테이블로 변환한다.

생성된 표는 현재 활성 노트의 커서 위치에 삽입한다.

## 4. Ribbon 버튼

플러그인 설치 시 Obsidian 왼쪽 Ribbon 영역에 실행 버튼을 추가한다.

아이콘은 재무제표, 표, 차트 느낌의 아이콘을 사용한다.

권장 아이콘은 다음 중 하나다.

| 아이콘                | 설명                    |
| ------------------ | --------------------- |
| `table`            | 표 생성 기능을 직관적으로 표현한다.  |
| `bar-chart-2`      | 재무 데이터, 실적 분석 느낌을 준다. |
| `file-spreadsheet` | 공시와 재무제표 느낌을 준다.      |

초기 버전은 `file-spreadsheet` 계열 아이콘을 우선 사용한다.

## 5. Modal 창

Ribbon 버튼을 클릭하면 DART 데이터 추출 Modal 창을 연다.

Modal 창은 다음 입력 요소를 가진다.

| 항목       | 입력 방식          | 설명                       |
| -------- | -------------- | ------------------------ |
| API Key  | Text input     | DART Open API Key를 입력한다. |
| DART URL | Text input     | DART 공시 페이지 URL을 입력한다.   |
| 추출 항목    | Checkbox group | 하나 이상의 추출 항목을 선택한다.      |
| 금액 단위    | Select box     | 억 또는 조를 선택한다.            |
| 실행       | Button         | 선택 항목을 조회하고 표를 생성한다.     |
| 취소       | Button         | Modal 창을 닫는다.            |

## 6. API Key 저장

API Key는 사용자가 한 번 입력하면 플러그인 설정에 저장한다.

다음 실행부터 저장된 API Key를 자동으로 불러온다.

API Key 입력창은 기본적으로 비밀번호 입력 형태로 표시한다.

사용자는 API Key를 수정할 수 있다.

수정된 API Key는 실행 시점 또는 저장 버튼 클릭 시 플러그인 설정에 다시 저장한다.

## 7. 입력 URL

사용자는 다음 형태의 DART 공시 URL을 입력한다.

```text
https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20260324001034
```

플러그인은 URL에서 `rcpNo` 값을 추출한다.

예시는 다음과 같다.

```text
rcpNo = 20260324001034
```

`rcpNo`는 공시 원문 조회와 공시 식별에 사용한다.

## 8. 추출 항목 선택

추출 항목은 다중 선택 방식으로 제공한다.

사용자는 다음 항목 중 하나 이상을 선택할 수 있다.

| 선택 항목     | 설명                   |
| --------- | -------------------- |
| 연결포괄손익계산서 | 연결 기준 포괄손익계산서를 조회한다. |
| 포괄손익계산서   | 별도 기준 포괄손익계산서를 조회한다. |
| 주식의 총수    | 주식 수 관련 표를 조회한다.     |

여러 항목을 선택하면 선택 항목을 순서대로 처리한다.

출력 순서는 Modal 화면에 표시된 순서를 따른다.

```text
1. 연결포괄손익계산서
2. 포괄손익계산서
3. 주식의 총수
```

## 9. 연결 데이터 처리 규칙

사용자가 `연결포괄손익계산서`를 선택하면 플러그인은 연결 기준 데이터만 조회한다.

연결포괄손익계산서 데이터가 존재하면 해당 데이터를 표로 생성한다.

연결포괄손익계산서 데이터가 존재하지 않으면 연결 데이터를 찾을 수 없다는 문장을 출력한다.

별도 기준 포괄손익계산서는 사용자가 `포괄손익계산서`를 직접 선택했을 때만 조회한다.

출력 예시는 다음과 같다.

```markdown
### 연결포괄손익계산서

연결포괄손익계산서 데이터를 찾을 수 없습니다.
```

## 10. 금액 단위 선택

금액 단위는 Modal 창에서 선택한다.

선택 가능한 단위는 다음과 같다.

| 단위 | 설명                    |
| -- | --------------------- |
| 억  | 원 단위 금액을 억원 단위로 변환한다. |
| 조  | 원 단위 금액을 조원 단위로 변환한다. |

기본값은 `억`으로 한다.

금액 단위 선택은 포괄손익계산서 계열 항목에 적용한다.

주식 수 항목은 수량 데이터이므로 금액 단위 변환을 적용하지 않는다.

## 11. 단위 변환 규칙

DART에서 가져온 금액은 내부적으로 원 단위 숫자로 정규화한다.

억원 단위는 다음 기준으로 계산한다.

```text
억원 = 원 / 100,000,000
```

조원 단위는 다음 기준으로 계산한다.

```text
조원 = 원 / 1,000,000,000,000
```

억원 단위는 정수로 표시한다.

조원 단위는 소수점 둘째 자리까지 표시한다.

표 위에는 선택한 단위를 표시한다.

## 12. 연결포괄손익계산서 추출 규칙

연결포괄손익계산서는 연결 기준 재무제표에서 포괄손익계산서 데이터를 추출한다.

초기 추출 항목은 다음과 같다.

| 표준 항목 | 매핑 후보             |
| ----- | ----------------- |
| 매출    | 매출액, 수익, 영업수익, 매출 |
| 영업이익  | 영업이익, 영업이익(손실)    |

출력 기간은 최근 3개년으로 한다.

연도 컬럼은 최신 연도부터 과거 연도 순서로 표시한다.

출력 예시는 다음과 같다.

```markdown
### 연결포괄손익계산서

단위: 억원

| 구분 | 2025년 | 2024년 | 2023년 |
| ---- | -----: | -----: | -----: |
| 매출 | 2,519 | 1,930 | 1,908 |
| 영업이익 | 241 | 102 | 131 |
```

## 13. 포괄손익계산서 추출 규칙

포괄손익계산서는 별도 기준 재무제표에서 포괄손익계산서 데이터를 추출한다.

초기 추출 항목은 연결포괄손익계산서와 동일하다.

| 표준 항목 | 매핑 후보             |
| ----- | ----------------- |
| 매출    | 매출액, 수익, 영업수익, 매출 |
| 영업이익  | 영업이익, 영업이익(손실)    |

출력 기간은 최근 3개년으로 한다.

출력 예시는 다음과 같다.

```markdown
### 포괄손익계산서

단위: 억원

| 구분 | 2025년 | 2024년 | 2023년 |
| ---- | -----: | -----: | -----: |
| 매출 | 2,300 | 1,820 | 1,760 |
| 영업이익 | 210 | 95 | 120 |
```

## 14. 주식의 총수 추출 규칙

주식의 총수는 공시 원문에서 주식 수 관련 표를 추출한다.

초기 추출 항목은 다음과 같다.

| 표준 항목           | 설명              |
| --------------- | --------------- |
| 발행할 주식의 총수      | 정관상 발행 가능한 주식 수 |
| 현재까지 발행한 주식의 총수 | 누적 발행 주식 수      |
| 현재 유통주식수        | 실제 유통 가능한 주식 수  |
| 자기주식수           | 회사가 보유한 자기주식 수  |

출력 예시는 다음과 같다.

```markdown
### 주식의 총수

| 구분 | 수량 |
| ---- | ---: |
| 발행할 주식의 총수 | 100,000,000 |
| 현재까지 발행한 주식의 총수 | 25,000,000 |
| 현재 유통주식수 | 24,500,000 |
| 자기주식수 | 500,000 |
```

## 15. 여러 항목 출력 형식

여러 항목을 선택하면 각 결과를 하나의 문자열로 합친 뒤 현재 커서 위치에 삽입한다.

각 항목은 제목과 표를 함께 출력한다.

항목 사이에는 빈 줄을 둔다.

출력 예시는 다음과 같다.

```markdown
### 연결포괄손익계산서

단위: 억원

| 구분 | 2025년 | 2024년 | 2023년 |
| ---- | -----: | -----: | -----: |
| 매출 | 2,519 | 1,930 | 1,908 |
| 영업이익 | 241 | 102 | 131 |

### 포괄손익계산서

단위: 억원

| 구분 | 2025년 | 2024년 | 2023년 |
| ---- | -----: | -----: | -----: |
| 매출 | 2,300 | 1,820 | 1,760 |
| 영업이익 | 210 | 95 | 120 |

### 주식의 총수

| 구분 | 수량 |
| ---- | ---: |
| 발행할 주식의 총수 | 100,000,000 |
| 현재까지 발행한 주식의 총수 | 25,000,000 |
| 현재 유통주식수 | 24,500,000 |
| 자기주식수 | 500,000 |
```

## 16. 노트 삽입 규칙

생성된 마크다운은 현재 활성화된 마크다운 편집기의 커서 위치에 삽입한다.

삽입 후 커서는 삽입된 텍스트 뒤로 이동한다.

활성 편집기가 있으면 현재 커서 위치를 사용한다.

활성 편집기가 없으면 안내 메시지를 표시한다.

## 17. 설정 구조

플러그인은 다음 설정값을 저장한다.

```ts
interface DartPluginSettings {
  apiKey: string;
  defaultUnit: "억" | "조";
}
```

기본 설정값은 다음과 같다.

```ts
const DEFAULT_SETTINGS: DartPluginSettings = {
  apiKey: "",
  defaultUnit: "억",
};
```

## 18. 내부 타입

추출 항목 타입은 다음과 같이 정의한다.

```ts
type ExtractType =
  | "consolidated_comprehensive_income"
  | "separate_comprehensive_income"
  | "stock_total";
```

금액 단위 타입은 다음과 같이 정의한다.

```ts
type AmountUnit = "억" | "조";
```

Modal 실행 입력값은 다음과 같이 정의한다.

```ts
interface DartExtractRequest {
  apiKey: string;
  dartUrl: string;
  rcpNo: string;
  extractTypes: ExtractType[];
  unit: AmountUnit;
}
```

포괄손익계산서 결과는 다음과 같이 정의한다.

```ts
interface IncomeStatementTable {
  title: string;
  unit: AmountUnit;
  years: string[];
  rows: {
    label: string;
    values: Record<string, number | null>;
  }[];
}
```

주식의 총수 결과는 다음과 같이 정의한다.

```ts
interface StockTotalTable {
  title: string;
  rows: {
    label: string;
    value: number | null;
  }[];
}
```

## 19. 주요 파일 구조

초기 구현 파일 구조는 다음과 같이 구성한다.

```text
obsidian-dart-plugin/
├─ manifest.json
├─ package.json
├─ tsconfig.json
├─ esbuild.config.mjs
├─ styles.css
└─ src/
   ├─ main.ts
   ├─ settings.ts
   ├─ dartModal.ts
   ├─ dartClient.ts
   ├─ dartUrl.ts
   ├─ extractors/
   │  ├─ incomeStatementExtractor.ts
   │  └─ stockTotalExtractor.ts
   ├─ formatters/
   │  ├─ amountFormatter.ts
   │  └─ markdownTableBuilder.ts
   └─ types.ts
```

## 20. 파일별 역할

### 20.1 main.ts

Obsidian 플러그인의 진입점이다.

설정을 로드한다.

Ribbon 버튼을 등록한다.

Modal 창을 실행한다.

생성된 마크다운을 현재 노트에 삽입한다.

### 20.2 settings.ts

플러그인 설정 로드와 저장을 담당한다.

API Key와 기본 금액 단위를 관리한다.

### 20.3 dartModal.ts

사용자 입력 UI를 담당한다.

API Key, DART URL, 추출 항목, 금액 단위를 입력받는다.

실행 버튼 클릭 시 입력값을 검증하고 추출 요청 객체를 생성한다.

### 20.4 dartClient.ts

DART API 호출을 담당한다.

공시 원문 조회, 재무제표 조회, 필요한 메타데이터 조회를 처리한다.

### 20.5 dartUrl.ts

DART URL에서 `rcpNo`를 추출한다.

URL 형식 검증을 담당한다.

### 20.6 incomeStatementExtractor.ts

연결포괄손익계산서와 포괄손익계산서 추출을 담당한다.

재무제표 응답에서 매출, 영업이익 항목을 찾아 표준 항목으로 변환한다.

### 20.7 stockTotalExtractor.ts

공시 원문에서 주식의 총수 관련 정보를 추출한다.

주식 수 항목명을 표준 항목명으로 변환한다.

### 20.8 amountFormatter.ts

원 단위 금액을 억원 또는 조원 단위로 변환한다.

숫자 포맷과 천 단위 콤마 처리를 담당한다.

### 20.9 markdownTableBuilder.ts

추출 결과를 마크다운 테이블 문자열로 변환한다.

여러 항목 결과를 하나의 마크다운 문자열로 병합한다.

### 20.10 types.ts

공통 타입을 정의한다.

## 21. 실행 흐름

전체 실행 흐름은 다음과 같다.

```text
플러그인 로드
→ 설정 로드
→ Ribbon 버튼 등록
→ 사용자가 Ribbon 버튼 클릭
→ Modal 창 열기
→ 저장된 API Key 표시
→ DART URL 입력
→ 추출 항목 다중 선택
→ 금액 단위 선택
→ 실행 버튼 클릭
→ 입력값 검증
→ rcpNo 추출
→ 선택 항목 순서대로 데이터 조회
→ 항목별 데이터 추출
→ 항목별 마크다운 테이블 생성
→ 전체 결과 병합
→ 현재 노트 커서 위치에 삽입
```

## 22. 입력값 검증

플러그인은 실행 전에 다음 값을 검증한다.

| 검증 항목    | 기준                           |
| -------- | ---------------------------- |
| API Key  | 빈 문자열이면 안내 메시지를 표시한다.        |
| DART URL | URL에서 `rcpNo`를 추출할 수 있어야 한다. |
| 추출 항목    | 하나 이상 선택되어야 한다.              |
| 금액 단위    | 억 또는 조 중 하나여야 한다.            |
| 활성 노트    | 현재 편집 가능한 마크다운 노트가 있어야 한다.   |

## 23. 오류 처리

오류 상황별 안내 메시지는 다음과 같다.

| 상황          | 안내 메시지                    |
| ----------- | ------------------------- |
| API Key 없음  | DART API Key를 입력해야 합니다.   |
| URL 형식 오류   | DART 공시 URL을 확인해야 합니다.    |
| rcpNo 추출 실패 | URL에서 접수번호를 찾을 수 없습니다.    |
| 추출 항목 없음    | 추출할 항목을 하나 이상 선택해야 합니다.   |
| 활성 노트 없음    | 테이블을 삽입할 노트를 열어야 합니다.     |
| DART 조회 실패  | DART 데이터를 조회할 수 없습니다.     |
| 연결 데이터 없음   | 연결포괄손익계산서 데이터를 찾을 수 없습니다. |
| 별도 데이터 없음   | 포괄손익계산서 데이터를 찾을 수 없습니다.   |
| 주식 수 데이터 없음 | 주식의 총수 데이터를 찾을 수 없습니다.    |

## 24. 초기 개발 범위

초기 버전은 다음 기능을 구현한다.

| 번호 | 기능                                 |
| -- | ---------------------------------- |
| 1  | Obsidian 플러그인 기본 구조 생성             |
| 2  | `obsidian-dart-plugin` manifest 구성 |
| 3  | Ribbon 버튼 추가                       |
| 4  | 재무제표 느낌의 아이콘 적용                    |
| 5  | Modal 창 생성                         |
| 6  | API Key 입력 UI 생성                   |
| 7  | API Key 저장 기능 구현                   |
| 8  | DART URL 입력 UI 생성                  |
| 9  | DART URL에서 `rcpNo` 추출              |
| 10 | 추출 항목 체크박스 UI 생성                   |
| 11 | 다중 선택 처리                           |
| 12 | 금액 단위 선택 UI 생성                     |
| 13 | 억원 단위 변환                           |
| 14 | 조원 단위 변환                           |
| 15 | 연결포괄손익계산서 추출                       |
| 16 | 포괄손익계산서 추출                         |
| 17 | 주식의 총수 추출                          |
| 18 | 마크다운 테이블 생성                        |
| 19 | 여러 표 연속 출력                         |
| 20 | 현재 노트 커서 위치 삽입                     |

## 25. 추후 확장 후보

추후 버전에서 다음 기능을 추가할 수 있다.

| 기능                      | 설명                                    |
| ----------------------- | ------------------------------------- |
| 재무상태표 추출                | 자산, 부채, 자본 항목을 표로 생성한다.               |
| 현금흐름표 추출                | 영업활동, 투자활동, 재무활동 현금흐름을 표로 생성한다.       |
| 당기순이익 추가                | 포괄손익계산서에 당기순이익을 추가한다.                 |
| 매출총이익 추가                | 포괄손익계산서에 매출총이익을 추가한다.                 |
| EPS 추가                  | 주당순이익 항목을 추가한다.                       |
| 다중 공시 URL 처리            | 여러 URL을 입력받아 한 번에 처리한다.               |
| 표 템플릿 설정                | 사용자가 출력 표 형식을 설정한다.                   |
| 회사명 자동 삽입               | 표 위에 회사명을 자동으로 표시한다.                  |
| 공시명 자동 삽입               | 표 위에 사업보고서, 반기보고서 등 공시명을 표시한다.        |
| 원본 링크 자동 삽입             | 표 아래에 DART 원본 링크를 추가한다.               |
| 전년 대비 증감률               | 각 항목의 YoY 증가율을 함께 표시한다.               |
| Obsidian frontmatter 연동 | 회사명, 종목코드, 기준연도 등을 frontmatter에 저장한다. |
